import { Client } from 'ssh2';

const conn = new Client();

conn.on('ready', () => {
    console.log('✅ SSH Conectado ao servidor Supabase');

    // Check IDs mismatch
    const email = 'ublochat@admin.com';
    const sql = `
SELECT 
    'auth.users' as source, id, email FROM auth.users WHERE email = '${email}'
UNION ALL
SELECT 
    'public.profiles' as source, id, email FROM public.profiles WHERE email = '${email}';
`;

    conn.exec(`docker exec $(docker ps -q -f name=supabase_db) psql -U postgres -d postgres -c "${sql}"`, (err, stream) => {
        if (err) throw err;

        let output = '';
        stream.on('data', (data) => output += data.toString());
        stream.stderr.on('data', (data) => output += data.toString());
        stream.on('close', () => {
            console.log('=== COMPARAÇÃO DE IDs ===');
            console.log(output);
            conn.end();
        });
    });
}).connect({ host: '194.163.189.247', port: 22, username: 'root', password: 'zlPnsbN8y37?Xyaw', readyTimeout: 120000 });
